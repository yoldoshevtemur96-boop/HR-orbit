export default function HomePage() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center px-6 text-center">
      <p className="text-xs font-semibold uppercase tracking-wide text-accent">HR Orbit</p>
      <h1 className="mt-2 font-display text-3xl font-semibold text-stone-900">
        Boshlang'ich skeleton tayyor
      </h1>
      <p className="mt-3 text-sm text-stone-500">
        Backend: <code>http://localhost:4000</code> · Login va Dashboard sahifalari keyingi bosqichda qo&apos;shiladi.
      </p>
    </main>
  );
}
